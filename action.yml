name: 'Hello World'
description: 'Greet someone'
runs:
  using: "composite"
  steps:
    - name: Chmod
      run: chmod +x gradlew
      shell: bash
    - name: Execute myTask
      run: ./gradlew myTask
      shell: bash
